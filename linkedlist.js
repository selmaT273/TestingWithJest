const LinkedListNode = require('./linkedlistnode');

class LinkedList {
    constructor() {
        this.size = 0;
        this.head = null;
        this.tail = null;
    }

    insert(value)
}

module.exports = LinkedList;
